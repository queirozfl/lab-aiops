flowchart TB
    subgraph HOST["🖥️ Workstation física — Windows + Hyper-V<br/>Xeon E3-1240 v3 · 32 GB RAM · SSD"]
        subgraph ZBX["VM zabbix-srv — Ubuntu 24.04<br/>2 vCPU · 4 GB RAM · 40 GB"]
            ZS["Zabbix Server<br/>porta 10051"]
            FE["Frontend Web (Nginx + PHP)<br/>porta 80"]
            DB[("MySQL")]
        end
        subgraph N8N["VM n8n-srv — Ubuntu 24.04<br/>2 vCPU · 2-3 GB RAM · 20 GB"]
            WF["n8n (Docker)<br/>porta 5678"]
            AG["🤖 Nó AI Agent<br/>(workflow n8n)"]
        end
        subgraph L1["VM lab-01 (opcional) · 1 GB"]
            A1["Zabbix Agent<br/>porta 10050"]
        end
        VSW{{"Switch Virtual (Externo)"}}
        NIC["Placa de rede física"]
    end

    LAN["Rede local (roteador/switch)"]
    PC["💻 Seu computador<br/>navegador + SSH"]
    LLM["☁️ API do LLM<br/>(Claude / OpenAI / Ollama local)"]
    NOTIF["📱 Notificações<br/>Telegram / Slack / e-mail"]

    ZS --> DB
    FE --> DB
    ZS -- "coleta (10050)" --> A1

    ZS -- "① alerta via webhook" --> WF
    WF --> AG
    AG -- "② analisa com" --> LLM
    AG -- "③ consulta/ação via API Zabbix" --> FE
    AG -- "④ envia diagnóstico" --> NOTIF

    ZBX --- VSW
    N8N --- VSW
    L1 --- VSW
    VSW --- NIC
    NIC --- LAN
    PC --- LAN
    LAN -.-> LLM
